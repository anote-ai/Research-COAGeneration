# Experiments

Venue-specific runners live in separate directories and call the shared
`metarouter` package. Shared statistical and export utilities should be placed
under `shared/` rather than copied between venues.
