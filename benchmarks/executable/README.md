# Executable Benchmark

Planned AAAI benchmark for real task inputs with machine-checkable outcomes.

Expected components:

- data-analysis tasks executed in a sandbox
- retrieval tasks over a frozen document corpus
- document extraction with labeled outputs
- held-out task templates and unavailable-tool conditions

Task licenses, splits, evaluators, and provenance must be documented here.
